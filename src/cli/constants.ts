/**
 * Constants shared between the CLI dispatcher and its sub-command modules.
 * Extracted to break a circular import between cli.ts and its siblings.
 */
export const MARKETPLACE_NAME = "a2a-bridge";
export const PLUGIN_NAME = "a2a-bridge";
